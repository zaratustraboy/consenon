let valueWithExplicit;

// Some logic that determines the value
if (someCondition) {
    valueWithExplicit = "Value A";
} else {
    valueWithExplicit = "Value B";
}

console.log(valueWithExplicit); // Outputs either "Value A" or "Value B" depending on the condition
